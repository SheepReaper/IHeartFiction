Use current C# features where practical, file-scoped namespaces, 4-space indentation from `.editorconfig`, and vertical slices with CQRS-lite organization. Warnings are errors. Prefer source-generated `[LoggerMessage]` partial methods. Keep server authorization and shared validation at API boundaries. Use WolverineFx only for asynchronous, cross-domain, or scalable queued work. Preserve `SocialPreviewMetadata.razor` as the single SEO `HeadContent` owner. `AGENTS.md` and the repository feature-workflow skill are authoritative for task-specific guardrails.
