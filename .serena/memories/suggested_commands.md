Run `tools/agent-bootstrap.ps1` on Windows or `tools/agent-bootstrap.sh` on Linux/cloud before `--no-restore` build/test or origin-dependent commands. Then use `dotnet build --configuration Debug --no-restore` and `dotnet test --configuration Release --no-restore`. Local tools are pinned in `.config/dotnet-tools.json`. Start with `aspire start`; inspect with `aspire describe`; use `aspire resource <name> rebuild` after source changes. Use the exact migration command from `AGENTS.md`. Force bootstrap source-generator packaging only after generator changes.
