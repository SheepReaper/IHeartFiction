After code changes, run the platform bootstrap before `--no-restore`, then `dotnet build --configuration Debug --no-restore` and proportionate tests with `dotnet test --configuration Release --no-restore`. Integration tests require Docker. Refresh `openapi.json` through the normal API build after contract changes, then build SharedWeb. A running Aspire resource must be rebuilt, not merely restarted, to compile source. Report browser, migration, authentication, and distributed-message verification separately from compilation.
