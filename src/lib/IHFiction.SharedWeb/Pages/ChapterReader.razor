@using IHFiction.SharedWeb.Components.Reading

@page "/stories/{storyId}/chapters/{chapterId}"

@attribute [Sitemap]

<PageTitle>Reading Chapter - IHeartFiction</PageTitle>

<StoryReader StoryId="@StoryId" CurrentChapterId="@ChapterId" />

@code {
    [Parameter] public string StoryId { get; set; } = null!;
    [Parameter] public string ChapterId { get; set; } = null!;
}
